import React from 'react';
import Button from '../../../../components/buttons/Button';

/**
 * Displays the button for submitting a build
 * @returns
 */
function SubmitBtn() {
	return <Button type="submit" text="Submit" color="btn-primary" icon="upload" />;
}

export default SubmitBtn;
