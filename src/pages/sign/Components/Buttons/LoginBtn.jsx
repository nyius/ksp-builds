import React from 'react';
import Button from '../../../../components/buttons/Button';

/**
 * Button for logging in
 * @returns
 */
function LoginBtn() {
	return <Button htmlFor="login-modal" text="Login" color="btn-primary" icon="login" />;
}

export default LoginBtn;
